[pytest]
testpaths = app32/tests
addopts = -p no:pytest_flask.plugin
markers =
    e2e: testes ponta a ponta com browser real
    smoke: cobertura mínima operacional
    dev_full: execução destrutiva autorizada apenas em DEV/HML
    prod_safe: execução segura para produção controlada
